<header class="header">
	<aside class="header__logo">Q</aside>
	<% if user_signed_in? %>
		<%= render "partials/navbar" %>
	<% end %>
	<%= image_tag('profile.jpg', class: 'header__avatar') %>
</header>
