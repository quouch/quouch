type: object
properties:
  code:
    type: integer
  message:
    type: string
    description: A human readable error message
    example: "Unauthorized"
  data:
    type: object
    properties:
      errors:
        # an array of strings
        type: array
        items:
          type: string
required:
  - code
  - message
